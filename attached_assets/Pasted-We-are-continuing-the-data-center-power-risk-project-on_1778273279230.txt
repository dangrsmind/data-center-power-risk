We are continuing the data-center-power-risk project on branch:

codex-coordinate-capture-correction

Codex added the coordinate capture/correction workflow.

Current observed issue:
The /map page loads, but no project markers appear and I do not see the coordinate edit features.

Important backend check:
GET http://localhost:8000/projects returns two projects with valid coordinates:

[
  {
    "id": "b12eefac-7c77-42df-8444-6d1c06ed801d",
    "canonical_name": "AVAIO Farmville",
    "developer": "AVAIO Digital",
    "state": "VA",
    "county": "Prince Edward",
    "latitude": 37.3021,
    "longitude": -78.3919,
    "coordinate_status": "unverified",
    "lifecycle_state": "candidate_unverified",
    "phase_count": 0,
    "current_hazard": 0.0655,
    "deadline_probability": 0.2464,
    "risk_tier": "low"
  },
  {
    "id": "37515520-bd3f-48bc-83e8-35cc5aa44cfb",
    "canonical_name": "CleanArc VA1",
    "developer": "CleanArc Data Centers",
    "state": "VA",
    "county": "Caroline",
    "latitude": 38.03,
    "longitude": -77.35,
    "coordinate_status": "unverified",
    "lifecycle_state": "candidate_unverified",
    "phase_count": 0,
    "current_hazard": 0.0655,
    "deadline_probability": 0.2464,
    "risk_tier": "low"
  }
]

Diagnosis:
The backend and database appear to be working. The likely problem is frontend map rendering or API data-shape mismatch.

Likely mismatch:
The map may be expecting:
- name instead of canonical_name
- lat instead of latitude
- lng/lon instead of longitude
- risk instead of risk_tier

Your task:
Fix the frontend map so it renders the actual backend /projects response.

Steps:

1. Confirm current branch:
   git branch --show-current
   git rev-parse --short HEAD

2. Confirm backend response:
   curl http://localhost:8000/projects

3. Inspect the frontend map code.
   Look for:
   - which endpoint /map calls
   - which project fields it expects
   - any filter that might incorrectly drop valid projects
   - any Leaflet errors caused by undefined lat/lon

4. Fix the map data adapter/rendering logic to use:
   - project.id
   - project.canonical_name
   - project.developer
   - project.state
   - project.county
   - project.latitude
   - project.longitude
   - project.coordinate_status
   - project.risk_tier
   - project.deadline_probability

5. Convert coordinates to numbers before passing them to Leaflet:
   const lat = Number(project.latitude)
   const lng = Number(project.longitude)

6. Render a marker only if:
   - latitude is not null
   - longitude is not null
   - Number.isFinite(lat)
   - Number.isFinite(lng)

7. Do not accidentally filter out valid coordinates because coordinate_status is "unverified".
   Unverified coordinates should still render, but should be labeled as unverified.

8. Marker popup should show:
   - canonical_name
   - developer
   - county/state
   - risk_tier
   - deadline_probability
   - coordinate_status
   - latitude/longitude
   - Edit coordinates button

9. Make sure the Edit coordinates button is visible in the marker popup and opens the coordinate editor.

10. If coordinate editing is only reachable from marker popups, add a visible fallback entry point for projects without markers:
    either:
    - a /coordinates route, or
    - a "Coordinate review" panel on /map.

The fallback should list projects from:
GET /projects/missing-coordinates

and allow Add/Edit coordinates.

Acceptance criteria:
- AVAIO Farmville appears on the map near 37.3021, -78.3919.
- CleanArc VA1 appears on the map near 38.03, -77.35.
- Clicking a marker opens a popup.
- The popup shows project and coordinate metadata.
- The popup includes an Edit coordinates button.
- Editing and saving coordinates moves the marker.
- Projects with missing coordinates have some visible way to add coordinates.
- Do not redesign the feature.
- Make the smallest fix needed.
- Do not merge to main.
- Commit fixes to the same branch:
  codex-coordinate-capture-correction

Before committing, run:
git status --short

Do not commit:
- local.db
- node_modules
- runtime CSV files
- runtime JSON files
- geocoding caches
- backend/data/exports/20260508/

Commit message:
Fix map rendering for coordinate workflow

Then push:
git pull --rebase origin codex-coordinate-capture-correction
git push origin codex-coordinate-capture-correction

Report back:
- final commit hash
- files changed
- whether markers now appear
- whether coordinate editing works
- anything still incomplete